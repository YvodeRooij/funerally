import { VenueBookingManager } from "@/components/features/bookings/venue-booking-manager"

export default function VenueBookingsPage() {
  return (
    <div className="min-h-screen bg-slate-50">
      <VenueBookingManager />
    </div>
  )
}
